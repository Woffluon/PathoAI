# Prometheus configuration for PathoAI monitoring
# This configuration scrapes metrics from the PathoAI application

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'pathoai'
    static_configs:
      - targets: ['pathoai:9090']
